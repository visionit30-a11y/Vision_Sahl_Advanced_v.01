# تقرير جاهزية بدء المشروع — منصة سهل المطور

**التاريخ:** 2026-09-05  
**المستودع المعتمد:** https://github.com/visionit30-a11y/Vision_Sahl_Advanced_v.01  
**مجلد العمل المكتشف فعليًا:** `D:\Sahal_Almotwar_v01`  
**الحالة:** لم يُكتب أي كود ولم يُنشأ أي ملف مشروع. التقرير للمراجعة والاعتماد فقط.

---

## 0. نتيجة فحص البيئة

| البند | النتيجة |
|---|---|
| المسار الوارد في تعليماتك | `E:\ضع-هنا-مسار-المشروع\Vision_Sahl_Advanced_v.01` — نص نائب لم يُستبدل، وغير موجود على القرص E |
| المسار الفعلي (بعد تأكيدك) | `D:\Sahal_Almotwar_v01` — موجود، وتم منحي صلاحية الوصول إليه |
| محتويات المجلد | ملفان فقط: BRD (53,735 بايت) و SRS (59,763 بايت). لا مجلدات فرعية، لا كود، لا `.git` |
| حالة Git المحلية | **لا يوجد مستودع Git** في المجلد (`fatal: not a git repository`) |
| المستودع البعيد | موجود ويمكن الوصول إليه، لكنه **فارغ تمامًا — لا يحتوي على أي فرع** (لا `main` ولا `develop`) |
| الوثيقتان | لم تُحذفا ولم تُعدَّلا. تمت قراءتهما فقط |

---

## 1. تأكيد القراءة

- **BRD-SAHEL-001 v1.0 — مقروء بالكامل** (19 بندًا + ملحق واجهات المستخدم والمحركات المشتركة + ملحق محرك شريط التنبيهات).
- **SRS-SAHEL-001 v1.0 — مقروء بالكامل** (26 بندًا + الملحقين نفسيهما).

---

## 2. فهم النطاق — بإيجاز

سهل المطور منصة سحابية **عربية أولًا، متعددة الجهات (Multi-Tenant SaaS)** للقطاع غير الربحي في السعودية، تُشغّلها شركة رؤى الحلول لتقنية المعلومات على قاعدة كود موحدة، وتخدم كل جمعية داخل بيئة معزولة في البيانات والهوية والصلاحيات والإعدادات.

**النطاق الملزم — 17 موديولًا:**
MT إدارة الجهات · GV الحوكمة · BN المستفيدون وإدارة الحالات · PJ المشاريع والبرامج والمنح · FI المالية والتبرعات والصناديق · PU المشتريات والموردون · IN المخزون والمستودعات · HR الموارد البشرية · VO التطوع · AS العهد والأصول · MG الاجتماعات والقرارات · RP التقارير ولوحات المؤشرات · NT الإشعارات · DM المستندات والأرشفة · WF الموافقات وسير العمل · IA الهوية والصلاحيات والتدقيق · INT التكاملات.

**القواعد العابرة التي فهمتها كقيود تنفيذ لا كتوصيات:**

1. كل سجل تشغيلي مرتبط بـ`tenant_id` ومحمي بـ RLS على مستوى قاعدة البيانات، لا بفلترة تطبيقية.
2. لا حذف صامت لمعاملة معتمدة — إلغاء/عكس/أرشفة حسب الموديول، وسجل تدقيق append-only.
3. الموافقات عبر محرك موحد واحد، لا منطق موافقات مكرر داخل كل شاشة.
4. المستندات في Object Storage والبيانات الوصفية في PostgreSQL — لا مجلدات محلية.
5. الأثر المالي ينتقل من خدمة المجال إلى المالية عبر عقد واضح — لا إعادة إدخال أرقام يدويًا.
6. مصدر حقيقة واحد لكل كيان مرجعي.
7. العربية افتراضية + RTL، والإنجليزية مدعومة من أول Commit، وكل نص عبر i18n مركزي.
8. Responsive حقيقي على الجوال/اللوحي/اللابتوب/سطح المكتب دون فقد وظيفة.
9. لا اختلاق تكامل حكومي — Interface + Mock/Stub + Mapping contract فقط حتى تتوفر الوثائق الرسمية.

---

## 3. القرارات التقنية المقفلة (كما استخرجتها)

| المعرف | الطبقة | الاختيار الملزم |
|---|---|---|
| TS-01 | Frontend | React + Vite |
| TS-02 | Backend | FastAPI (Python) |
| TS-03 | Database | PostgreSQL |
| TS-04 | عزل المستأجرين | PostgreSQL Row-Level Security (RLS) — إلزامي على مستوى القاعدة |
| TS-05 | Cache / Queue | Redis |
| TS-06 | المستندات | OCI Object Storage (أو Object Storage متوافق في بيئة التطوير) |
| TS-07 | السحابة | Oracle Cloud Infrastructure داخل المملكة |
| TS-08 | الحاويات | Docker + OKE |
| TS-09 | المعمارية | Modular Monolith |
| TS-10 | الأسرار | Environment + OCI Vault |
| TS-11 | IaC | Terraform |

**قرارات منتج مقفلة إضافية:** العربية افتراضية عند أول دخول · ثنائية اللغة من البنية الأولى · IBM Plex Sans Arabic خطًا وحيدًا للغتين · دعم هجري/ميلادي مع تخزين تاريخ قياسي · هوية بصرية مؤسسية سعودية الطابع دون محاكاة جهة حكومية بعينها.

**قيود قاعدة البيانات:** كل تغيير Schema عبر Migration متسلسلة موثقة · ممنوع DROP/Reset/Seed كامل كحل لمشكلة Migration · Backfill قبل فرض NOT NULL أو قيد جديد · معاملات قاعدة بيانات للعمليات المالية والاعتمادية.

**متطلبات غير وظيفية حاكمة:** P95 قرابة الثانية للشاشات التفاعلية · التقارير الثقيلة Async · توفر 99.9% · API عديمة الحالة · TLS + تشفير مخزَّن · تشفير حقلي للهوية الوطنية والبيانات البنكية · إقامة البيانات داخل المملكة · Logs/metrics/traces بوسوم tenant/correlation دون بيانات حساسة.

---

## 4. الممنوعات المستخرجة

**تقنيًا:** Next.js / Angular / Vue / أي Server-rendered framework بديل · Django / Flask / Node / NestJS / Laravel كخلفية · MySQL / MariaDB / SQLite / MongoDB كقاعدة تشغيل · Cache أو Queue بديل عن Redis · AWS / Azure / GCP كتصميم إنتاج · Microservices أو Event-driven distributed كإعادة تصميم تلقائية.

**في الواجهة:** Tailwind CSS · Bootstrap · أي إطار CSS Utility/Component بديل يؤدي الدور نفسه.

**Node.js:** مسموح فقط لتشغيل أدوات React + Vite وإدارة حزم الواجهة. ممنوع إنشاء API أو Business Logic أو Jobs خلفية به.

**سلوكيًا:** تعطيل RLS للتسهيل · الاكتفاء بفلترة `tenant_id` في الكود · تخزين ملفات الإنتاج في `uploads` محلي · حذف سجل تدقيق أو تعديل قيد معتمد مباشرة · Commit لأي أسرار أو `.env` أو بيانات شخصية · تغيير أسماء API/حقول/جداول دون خطة توافق · إعادة بناء المشروع بعد Phase 0 لمجرد تحسين التنظيم · اختلاق نتيجة اختبار أو ادعاء عمل وظيفة دون تحقق · تغيير التصميم أو الهوية خارج نطاق المهمة.

---

## 5. مراحل التنفيذ كما وردت في الوثيقتين

### 5.1 خطة BRD §16 — خمس مراحل (منظور الأعمال)

| المرحلة | النطاق |
|---|---|
| 0 — الأساس | هيكل المستودع، بيئات التشغيل، Multi-Tenancy، RLS، الهوية، RBAC، التدقيق، المستندات، Workflow، الإعدادات المشتركة |
| 1 — الامتثال والمالية الأساسية | دليل الحسابات، الصناديق، القيود، مراكز التكلفة، الحوكمة، الاجتماعات، الإفصاحات الأساسية |
| 2 — تقديم الخدمات | المستفيدون، المشاريع والبرامج، الإشعارات، قياس الأثر الأساسي |
| 3 — العمليات | المشتريات، المخزون، الموارد البشرية، التطوع، العهد والأصول |
| 4 — التكامل والتوسع | التكاملات الحكومية والقنوات، التحليلات المتقدمة، الأداء والتوسع |

### 5.2 خطة SRS §15 — اثنتا عشرة مرحلة (منظور التنفيذ)

| المرحلة | النطاق الإلزامي | بوابة الخروج |
|---|---|---|
| Phase 0 | Git/GitHub والفروع، الهيكل، React+Vite، FastAPI، PostgreSQL، Redis، Docker/local services، env example، README PowerShell، lint/test/typecheck | تشغيل كل الطبقات محليًا من تعليمات موثقة + أول Push مستقر |
| Phase 1 | Design System، IBM Plex Sans Arabic، tokens، layout، responsive shell، i18n، RTL/LTR، navigation shell | Shell يعمل باللغتين وعلى كل الأحجام بلا منطق أعمال وهمي |
| Phase 2 | Tenant model، auth، users/memberships/roles/permissions، RLS، audit foundation، tenant resolution | اختبارات تثبت منع cross-tenant access ونجاح الصلاحيات والتدقيق |
| Phase 3 | Workflow/approvals، documents abstraction، notifications events، settings، reference data، background jobs | خدمات مشتركة مختبرة بلا تكرار داخل الموديولات |
| Phase 4 | الحوكمة والاجتماعات | دورة حوكمة كاملة بصلاحيات وتدقيق ومرفقات |
| Phase 5 | Finance Core: COA، journals، funds، cost centers، budgets، أساس الحملات/التبرعات | قيد مزدوج متوازن + عزل + audit + قواعد funds |
| Phase 6 | المستفيدون وإدارة الحالات | سيناريو تسجيل–تقييم–اعتماد–مساعدة كامل ومختبر |
| Phase 7 | المشاريع والبرامج والمنح | مشروع مرتبط بالميزانية والمستفيدين والتقارير |
| Phase 8 | المشتريات والمخزون | Procure-to-receive وربط مالي/مخزني صحيح |
| Phase 9 | HR، التطوع، العهد والأصول | دورات HR والعهد بصلاحيات وتدقيق وروابط مالية |
| Phase 10 | التقارير والتكاملات والتقوية | حزمة قبول متكاملة قبل Staging |
| Phase 11 | OCI/OKE Staging والإنتاج | Staging قابل للتحقق، ثم الإنتاج بموافقتك |

> الخطتان ليستا متطابقتين في التقسيم ولا في الترتيب. راجع بند التعارضات (§8) والسؤال A-1.

---

## 6. المكونات والمحركات المشتركة الواجب تأسيسها

**محركات الواجهة**

1. **محرك الجداول والاستعلام** — بحث، فرز، فلاتر، Pagination من الخادم، اختيار سجل/عدة سجلات/الكل ضمن النطاق المسموح، عدّاد المحدد، الإجراءات الجماعية المصرح بها، إظهار/إخفاء/إعادة ترتيب الأعمدة، حفظ تفضيلات المستخدم لكل شاشة واستعادتها تلقائيًا، واحترام الصلاحيات والفلاتر الحالية.
2. **محرك التقارير والتصدير** — عرض في المتصفح، طباعة، PDF، Excel قابل للاستخدام، تصدير الكل أو المحدد، احترام اللغة والاتجاه والأعمدة والفلاتر والصلاحيات، ومنع كشف أي حقل محجوب بصلاحية، مع تنفيذ غير متزامن للتقارير الكبيرة.
3. **محرك شريط التنبيهات والحالة الموحد** — مساحة محجوزة مسبقًا في Layout بحيث لا يحدث Layout Shift عند الظهور/الاختفاء، ألوان هادئة حسب نوع الرسالة، صياغة قصيرة غير تقنية، روابط ذكية بإبراز رقم السجل (Bold ولون متناسق) تُفتح في تبويب مستقل دون فقد حالة الشاشة الحالية، حالات قابلة/غير قابلة للإغلاق، مدة ظهور تلقائية للرسائل البسيطة، إجراء «تراجع» عند دعم الخلفية له بأمان، احترام الصلاحيات، وi18n كامل، وAPI موحد (النوع، النص، الرابط، معرف السجل، المدة، قابلية الإغلاق، التراجع، الأهمية).
4. **مكتبة المكونات ونظام التصميم** — الأزرار وحالاتها وأحجامها وترتيبها، الحقول، البحث والفلاتر، النوافذ/المودال، رسائل التحقق قرب الحقل، حالات Loading/Empty/Error/Success، Breakpoints وTokens مركزية، Typography scale واحد.
5. **طبقة i18n مركزية** — لا نص ظاهر Hard-coded، تبديل لغة مع حفظ التفضيل، `dir` تلقائي، واعتماد Logical CSS properties لتجنب ازدواج CSS.

**خدمات الخلفية المشتركة**

6. **Identity / RBAC** — أدوار وصلاحيات قابلة للتهيئة لكل جهة، وSegregation of Duties للعمليات الحساسة، وMFA حسب السياسة.
7. **Tenant Context / RLS Guard** — تعيين سياق المستأجر الموثوق داخل نطاق كل Transaction قبل أي استعلام، مع دور قاعدة بيانات بلا BYPASSRLS.
8. **Workflow / Approvals** — مراحل، حدود مالية، تفويض، بديل، انتهاء.
9. **Audit** — append-only يسجل actor, tenant, timestamp, entity, action, before/after أو delta, correlation id.
10. **Documents** — Metadata في PostgreSQL، المحتوى في Object Storage، مفاتيح غير قابلة للتخمين، Signed URLs، تحقق من الحجم والنوع والمحتوى، واحترام متطلبات الاحتفاظ عند الحذف.
11. **Notifications** — من أحداث النظام عبر خدمة مركزية بقوالب لكل جهة، ودون إفساد المعاملة الأساسية عند فشل مسار غير حرج.
12. **Background Jobs / Queue** — على Redis، مهام Idempotent أو بمفتاح منع تكرار، وفشل غير حرج لا يُفسد معاملة PostgreSQL مكتملة.
13. **Integration Adapters** — واجهات قابلة للاستبدال + Mock/Stub، مصنّفة «غير متصلة» حتى تتوفر وثائق رسمية.

> ملتزم بقاعدة الوثيقتين: يُنشأ المحرك المشترك عندما يكون السلوك عامًا فعلًا وقابلًا لإعادة الاستخدام، مع API واضح واختبارات — دون Abstraction مبكر أو طبقات معقدة.

---

## 7. استراتيجية Git / GitHub التي سألتزم بها

- **الحساب:** `visionit30-a11y` · **المستودع:** `Vision_Sahl_Advanced_v.01`
- `main` — مستقر فقط، يستقبل المراحل/الإصدارات المقبولة. لا تطوير مباشر عليه إطلاقًا.
- `develop` — فرع التكامل الرئيسي.
- `feature/<short-name>` — فرع مستقل لكل مهمة، مشتق من `develop`، ولا يجمع مهام غير مترابطة.
- **PR** من `feature` إلى `develop` بعد Build/Test/Lint/Type-check، ثم دمج الإصدار المقبول من `develop` إلى `main` (SRS DEV-03).
- **Commits** ذرّية برسائل واضحة تصف التغيير الفعلي — لا Commit ضخم بعنوان مبهم.
- `.gitignore` من أول Commit: `.env`، البيئات الافتراضية، `node_modules`، مخرجات البناء، الـcaches، ملفات الـIDE المؤقتة.
- `.env.example` بلا أسرار، وREADME بأوامر PowerShell قابلة للنسخ.
- **Push** بعد قبول كل مرحلة وقبل بدء التالية.
- فرع المرحلة الأولى المقترح: `feature/phase-0-foundation`.

---

## 8. التعارضات والفجوات التي وجدتها بين BRD و SRS

**ت-1 — تعارض حقيقي في تقسيم المراحل وترتيبها (يمنع البدء بلا حسم).**  
BRD §16 يضع في «المرحلة 0 — الأساس» كامل حزمة: هيكل المستودع + بيئات التشغيل + Multi-Tenancy + RLS + الهوية + RBAC + التدقيق + **المستندات + Workflow + الإعدادات المشتركة**. بينما SRS §15 يوزّع المحتوى نفسه على أربع مراحل مستقلة لكل منها بوابة خروج خاصة: Phase 0 (المستودع والبيئة المحلية) ← Phase 1 (نظام التصميم والقشرة ثنائية اللغة) ← Phase 2 (Tenancy/Identity/RBAC/RLS/Audit) ← Phase 3 (الخدمات المشتركة). وتنفيذ «مرحلة 0» بمعناها في BRD يعني عمليًا خرق قاعدة SRS التي تمنع بدء مرحلة قبل إغلاق سابقتها.

**ت-2 — اختلاف في ترتيب الحوكمة مقابل المالية.**  
BRD يضع الحوكمة والاجتماعات في المرحلة 1 **مع** المالية الأساسية. SRS يفصلهما ويقدّم الحوكمة والاجتماعات (Phase 4) **قبل** Finance Core (Phase 5).

**ت-3 — موضع الإشعارات.**  
BRD يضع الإشعارات ضمن المرحلة 2 (تقديم الخدمات) مع المستفيدين والمشاريع. SRS يضعها ضمن الخدمات المشتركة في Phase 3، أي قبل موديولات الخدمات.

**ت-4 — نظام التصميم غير مذكور كمرحلة في BRD.**  
SRS يخصص Phase 1 كاملة لـ Design System والقشرة ثنائية اللغة قبل أي منطق أعمال، بينما BRD يدرج متطلبات اللغة والواجهة كعقد عام (§10) دون تخصيص مرحلة. لا تعارض في المضمون، لكن الفجوة تؤثر على الجدول الزمني والتسليمات.

**ت-5 — فرق في اسم المشروع/المجلد لا في الوثائق.**  
المسار الذي أعطيتني إياه في التعليمات (`E:\ضع-هنا-مسار-المشروع\Vision_Sahl_Advanced_v.01`) لا يطابق المجلد الفعلي (`D:\Sahal_Almotwar_v01`) ولا اسم المستودع. مسألة تشغيلية لا تعاقدية، لكنها تحتاج حسمًا قبل `git init`.

**لا تعارض** وجدته في: الـStack المقفل، الممنوعات، قواعد RLS والعزل، قواعد Git، عقد اللغة والخط والاستجابة، ملحق المحركات المشتركة، وملحق شريط التنبيهات — الملحقان واردان بنص متطابق في الوثيقتين.

---

## 9. القرارات المفتوحة التي أحتاج حسمها منك

> لن أبدأ أي جزء يتأثر بهذه الأسئلة قبل إجابتك، ولن أعتبر الصمت تفويضًا.

### المجموعة (أ) — حوكمة التنفيذ والبيئة

**A-1. أي خطة مراحل هي المرجع التنفيذي؟**  
*لماذا الآن:* تحدد ما أسلّمه أولًا وبوابة قبوله.  
*الخيارات:* (1) اعتماد مراحل SRS الاثنتي عشرة كخطة تنفيذ، ومراحل BRD الخمس كتجميع أعمال/فوترة أعلى منها. (2) اعتماد مراحل BRD الخمس حرفيًا. (3) خطة مدمجة تكتبها أنت.  
*توصيتي:* الخيار (1) — SRS هو المستند التقني التنفيذي وله بوابات خروج قابلة للقياس، وBRD نفسه يحيل التفصيل البرمجي إلى SRS. ويظل تسليم مراحل BRD مكفولًا لأن مجموع Phases 0–3 في SRS يساوي «المرحلة 0» في BRD.  
*الأثر:* الخيار (2) يعني مرحلة أولى ضخمة بلا بوابة قبول وسيطة، ويصعّب المراجعة والرجوع.

**A-2. مسار العمل النهائي واسم المجلد.**  
*الخيارات:* (1) العمل داخل `D:\Sahal_Almotwar_v01` كما هو. (2) إعادة تسميته إلى `Vision_Sahl_Advanced_v.01`. (3) مسار آخر تحدده.  
*توصيتي:* (1) — اسم المجلد المحلي لا يؤثر تقنيًا، و`origin` سيشير إلى المستودع المعتمد. أنقل الوثيقتين إلى `docs/` داخل المستودع مع الإبقاء على نسختيهما الحاليتين دون حذف.

**A-3. توزيع المهام بيني وبينك في التنفيذ.**  
*الوضع:* أستطيع إنشاء وتعديل الملفات داخل مجلد المشروع مباشرة، لكن **لا أستطيع** تشغيل PowerShell على ويندوز، ولا تثبيت Node/Python/PostgreSQL/Redis/Docker، ولا تنفيذ `git push` بهويتك.  
*توصيتي:* أكتب أنا كل الملفات والتهيئة والاختبارات، وأزوّدك بأوامر PowerShell جاهزة للنسخ لكل خطوة (تثبيت، تشغيل، Migration، Test، Commit، Push)، وتنفّذها أنت وتعيد لي المخرجات لأعالج أي فشل.  
*الأثر:* إن أردت مسارًا مختلفًا (مثل تشغيلي للأوامر عبر تفويض إضافي) فحدده الآن لأن README وأسلوب التوثيق يُبنى عليه.

**A-4. آلية Push والـPull Requests.**  
المستودع البعيد **فارغ بالكامل** ولا يحتوي أي فرع. من ينشئ `main` و`develop` ويدفعهما؟ وهل تريد PR رسميًا لكل feature (كما يوجب SRS DEV-03) رغم أن الفريق فرد واحد، أم دمجًا محليًا مع Push مباشر إلى `develop`؟  
*توصيتي:* الالتزام بالـPR كما نص SRS — يعطي سجل مراجعة وبوابة قبول لكل مرحلة، وتكلفته دقائق.

**A-5. Docker على جهاز التطوير.**  
هل Docker Desktop مثبت وشغّال على ويندوز؟  
*توصيتي:* استخدام `docker-compose` لتشغيل PostgreSQL + Redis (+ MinIO لاحقًا) — يضمن تطابق الإصدارات مع الإنتاج ويجنّبنا فروق التثبيت الأصلي. البديل عند عدم توفره: تثبيت أصلي لـ PostgreSQL و Redis على ويندوز، وهو أضعف تطابقًا مع OKE.

### المجموعة (ب) — بنية المستودع وقاعدة البيانات

**B-1. موضع مجلد الموديولات.**  
SRS §4 يذكر `modules/<domain>` كمسار منطقي دون تحديد ما إذا كان في الجذر أم داخل الخلفية.  
*الخيارات:* (1) `apps/api/app/modules/<domain>` (باكج بايثون واحد قابل للاستيراد داخل الـModular Monolith). (2) `modules/<domain>` في الجذر كحزم مستقلة.  
*توصيتي:* (1) — يبقي حزمة بايثون واحدة، ويحفظ حدود الموديولات بقواعد استيراد وفحص آلي، ويطابق Modular Monolith دون تعقيد التغليف. تبقى `migrations/` و`infra/` و`docs/` و`tests/` في الجذر كما نص SRS.

**B-2. طبقة الوصول للبيانات وأداة المهاجرات.**  
غير محددة في الوثيقتين.  
*الخيارات:* SQLAlchemy 2.x + Alembic · SQLModel + Alembic · SQL خام + أداة migration مستقلة.  
*توصيتي:* **SQLAlchemy 2.x + Alembic** — المعيار الفعلي مع FastAPI، ويدعم ضبط سياق RLS على مستوى الجلسة/المعاملة، وAlembic يوفر المهاجرات المتسلسلة والقابلة للرجوع التي توجبها الوثيقتان.  
*الأثر:* قرار غير قابل للتراجع عمليًا بعد أول Migration.

**B-3. إصدارات المنصات.**  
غير محددة.  
*توصيتي:* Python 3.12 · Node.js 20 LTS · PostgreSQL 16 · Redis 7.  
*الأثر:* تُثبَّت في `docker-compose` و`README` وملفات CI ولاحقًا في OKE.

### المجموعة (ج) — قرارات الواجهة (لازمة قبل Phase 1، وبعضها يُثبَّت في Phase 0)

**C-1. TypeScript أم JavaScript للواجهة؟**  
*توصيتي:* **TypeScript** — Type-check مطلوب صراحة في بوابة Phase 0، وعقود API من OpenAPI تُولَّد كأنواع، وهو ما يحمي مشروعًا بهذا الحجم من كسر العقود.  
*الأثر:* التحويل لاحقًا مكلف جدًا.

**C-2. أسلوب CSS البديل عن Tailwind/Bootstrap.**  
*الخيارات:* (1) CSS Modules + Design Tokens بمتغيرات CSS. (2) vanilla-extract. (3) CSS-in-JS (styled-components / Emotion).  
*توصيتي:* (1) — CSS منظم فعلًا، يدعم Logical Properties لـRTL/LTR بلا ازدواج، بلا تكلفة وقت تشغيل، ويطابق نص الوثيقتين حرفيًا.

**C-3. حزمة مكتبات الواجهة الأساسية (تُوثَّق في سجل التبعيات).**  
*لماذا الآن:* تحديدها لاحقًا يعني إعادة كتابة الشاشات.  
*المقترح:* React Router (التنقل) · TanStack Query (جلب البيانات والتخزين المؤقت) · TanStack Table **headless** كنواة لمحرك الجداول (بلا أي CSS، فلا يخالف حظر أطر الواجهة) · react-hook-form + zod (النماذج والتحقق) · لا مكتبة UI جاهزة إطلاقًا — المكونات من نظام تصميمنا.  
*البديل:* بناء محرك الجداول من الصفر بالكامل — أدق تحكمًا وأبطأ تسليمًا بفارق معتبر.  
*قرارك المطلوب:* الموافقة على الحزمة كاملة، أو استبعاد أي عنصر منها.

**C-4. مكتبة i18n.**  
*الخيارات:* react-i18next (+ i18next) · حل داخلي خفيف.  
*توصيتي:* **react-i18next** — ناضج، يدعم namespaces والجمع والاستيفاء وتحميل الترجمات كسولًا، ويمنع النصوص الثابتة.

**C-5. الهوية البصرية.**  
*لماذا الآن:* Phase 1 لا تُغلق بلا لوحة ألوان وTokens معتمدة.  
*المطلوب منك:* هل يوجد شعار أو لوحة ألوان معتمدة لـ«سهل المطور» / رؤى الحلول؟ وإن لم توجد، هل أقترح عليك 2–3 لوحات مؤسسية هادئة (بلا محاكاة جهة حكومية) للاختيار قبل بدء Phase 1؟

**C-6. مكتبة الأيقونات.**  
البند يوجب أيقونات ملونة معبرة ومتناسقة الأسلوب.  
*توصيتي:* مجموعة SVG واحدة موحدة الأسلوب (مثل Lucide/Phosphor كأساس شكلي) مع طبقة ألوان من Tokens المشروع، بدل خلط مصادر متنافرة. أنتظر قرارك لأن هذا يمس الهوية.

### المجموعة (د) — قرارات لاحقة لكنها تؤثر على تصميم Phase 0

**D-1. الاختبارات.** *توصيتي:* pytest + pytest-asyncio + httpx للخلفية · Vitest + Testing Library للواجهة · Playwright لاحقًا لـE2E. تُهيَّأ الهياكل في Phase 0 مع اختبار دخان لكل طبقة.

**D-2. أدوات الجودة.** *توصيتي:* Ruff (lint + format) + mypy للخلفية · ESLint + Prettier + `tsc --noEmit` للواجهة. مطلوبة صراحة في بوابة Phase 0.

**D-3. CI من Phase 0 أم لاحقًا؟** SRS يذكر CI/CD gates في Phase 11 فقط. *توصيتي:* GitHub Actions بسيط من Phase 0 (lint + typecheck + tests على كل PR) — يحمي بوابات القبول من البداية بتكلفة ضئيلة، ويُوسَّع في Phase 11.

**D-4. Object Storage محليًا.** *توصيتي:* MinIO عبر Docker عند الوصول إلى Phase 3، مع Interface واحد يبدّل بين MinIO وOCI Object Storage بالإعداد فقط. لا حاجة له في Phase 0.

**D-5. المصادقة وتحديد المستأجر (لازم قبل Phase 2 لا قبل Phase 0).** أحتاج لاحقًا قرارك في: JWT بـ HttpOnly cookie أم Authorization header · مدة الـaccess/refresh · آلية تحديد الجهة (نطاق فرعي لكل جمعية / رأس HTTP / claim داخل التوكن) · وهل يُخطَّط لاحقًا لربط «نفاذ» كمزود هوية. سأطرحه بتفصيله عند اقتراب Phase 2.

**D-6. محرك PDF العربي.** التصيير العربي RTL في PDF نقطة تقنية حساسة. *توصيتي المبدئية:* WeasyPrint (HTML→PDF) لأنه يعيد استخدام قوالب الواجهة ويدعم الخط والاتجاه. القرار النهائي عند مرحلة محرك التقارير.

---

## 10. ما سأنفذه تحديدًا في المرحلة الأولى (Phase 0) — بعد أمرك الصريح

**الفرع:** `feature/phase-0-foundation` من `develop`.

1. تهيئة Git محليًا، إنشاء `main` و`develop`، ربط `origin` بالمستودع المعتمد، ودفع أول Commit مستقر.
2. `.gitignore` شامل من أول Commit + `.env.example` بلا أسرار.
3. هيكل المستودع وفق SRS §4: `apps/web`, `apps/api`, `migrations`, `infra/terraform` (هيكل فارغ موثق)، `docs`, `tests`.
4. نقل نسخة من BRD و SRS إلى `docs/` دون حذف الأصلين.
5. **الخلفية:** هيكل FastAPI — إعدادات من متغيرات البيئة، Routers/Services/Dependencies كطبقات، شكل خطأ موحد بـcode ورسالة، logging منظم بـcorrelation id، ونقطتا `/health` و`/health/db`. **بلا أي منطق أعمال ولا موديولات مجال.**
6. **الواجهة:** هيكل React + Vite — بنية مجلدات، عميل API، صفحة تحقق واحدة تستدعي `/health`. **بلا نظام تصميم ولا شاشات — هذه Phase 1.**
7. **الخدمات المحلية:** `docker-compose.yml` بـ PostgreSQL و Redis، ومستخدم/دور قاعدة بيانات للتطبيق **بلا** `BYPASSRLS` ولا `SUPERUSER` منذ اللحظة الأولى (تمهيدًا لـ Phase 2).
8. **المهاجرات:** تهيئة Alembic وMigration أساس فارغة قابلة للتطبيق والرجوع — دون أي جدول مجال.
9. **الجودة:** Ruff + mypy + pytest للخلفية، ESLint + Prettier + `tsc --noEmit` + Vitest للواجهة، واختبار دخان لكل طبقة.
10. **CI:** Workflow بسيط على GitHub Actions (رهن القرار D-3).
11. **README:** بأوامر PowerShell قابلة للنسخ لكل شيء: التثبيت، رفع الخدمات، تشغيل الخلفية والواجهة، تشغيل المهاجرات، تشغيل الاختبارات والفحوص — مع بقاء الـRuntime عابرًا للمنصات (لا مسارات ويندوز ولا أوامر PowerShell داخل كود التشغيل).
12. **سجل التبعيات** (`docs/dependencies.md`) بكل مكتبة ومبرر إدخالها، و`docs/adr/` لقرارات المعمارية.
13. تقرير تغيير بكل ملف أنشئ ومساره وسببه، ثم Commits ذرّية، وPR إلى `develop`.

**خارج نطاق Phase 0 صراحة:** نظام التصميم، الشاشات، i18n الكامل، Tenancy، RLS، المصادقة، الصلاحيات، التدقيق، الموافقات، المستندات، الإشعارات، وأي جدول مجال.

---

## 11. معايير القبول التي سأغلق بها Phase 0

| # | المعيار | طريقة التحقق |
|---|---|---|
| 1 | المستودع البعيد يحتوي `main` و`develop`، والعمل تم على `feature/phase-0-foundation` | فحص الفروع على GitHub |
| 2 | لا يوجد أي سر أو `.env` أو مفتاح في تاريخ Git | فحص `git log` والـdiff + `.gitignore` |
| 3 | `docker compose up` يشغّل PostgreSQL و Redis بنجاح | أمر PowerShell موثق في README ينفَّذ من مجلد نظيف |
| 4 | الخلفية تعمل و`/health` و`/health/db` تعيدان استجابة صحيحة | استدعاء موثق + لقطة نتيجة |
| 5 | الواجهة تعمل وتستدعي الخلفية بنجاح | تشغيل `npm run dev` والتحقق |
| 6 | Alembic يطبّق Migration الأساس ويتراجع عنها بنجاح | `alembic upgrade head` ثم `downgrade` |
| 7 | دور قاعدة بيانات التطبيق بلا `BYPASSRLS` وبلا `SUPERUSER` | استعلام تحقق موثق |
| 8 | `ruff` + `mypy` + `pytest` تمر بلا أخطاء | مخرجات التنفيذ |
| 9 | `eslint` + `tsc --noEmit` + `vitest` تمر بلا أخطاء | مخرجات التنفيذ |
| 10 | README ينفَّذ حرفيًا على جهاز نظيف بأوامر PowerShell دون خطوات ضمنية | تنفيذك أنت للتعليمات |
| 11 | لا يوجد أي منطق أعمال أو شاشة أو جدول مجال — Phase 0 تأسيس فقط | مراجعة قائمة الملفات |
| 12 | تقرير تغيير يحدد كل ملف ومساره وسبب إنشائه + Commits ذرّية + PR مفتوح | مراجعتك للـPR |

---

## 12. الوضع الحالي

توقفت هنا كما أمرت. لم أُنشئ ملفًا واحدًا، ولم أثبّت مكتبة، ولم ألمس الوثيقتين، ولم أنفّذ `git init`.

**أنتظر منك:** إجابات المجموعات (أ) و(ب) و(ج) — أو على الأقل A-1 إلى A-5 و B-1 إلى B-3 و C-1 إلى C-4 لأتمكن من بدء Phase 0 — ثم **أمرًا صريحًا بالبدء**.
