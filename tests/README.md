# الاختبارات العابرة للطبقات

هذا المجلد مخصص لاختبارات التكامل وE2E التي تتجاوز طبقة واحدة (SRS §19)، ويبدأ استخدامه
فعليًا عند وجود سلوك عابر للطبقات — بدءًا من اختبارات عزل المستأجرين في **Phase 2**.

- اختبارات وحدة الخلفية: `apps/api/tests/`
- اختبارات الواجهة: `apps/web/src/__tests__/`
